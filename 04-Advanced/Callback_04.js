// This is what is referred to as Inversion of Control(IoC).
// You are handing over control of invoking future callbacks to a function you never wrote

// How do we fix/avoid this? One way is to use promises.